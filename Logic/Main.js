let outerUserInput = document.getElementById("UserInput").value;

function gameStart(startVal){
    startVal = startVal.toLowerCase();
    if(startVal === "start"){
        while(startVal === "start"){
            document.alert("test");
        }
    }
}

gameStart(outerUserInput);